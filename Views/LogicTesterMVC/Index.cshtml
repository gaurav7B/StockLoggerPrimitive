@* <!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Date Picker Form</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f9fc;
            color: #333;
            margin: 0;
            padding: 0;
            display: flex;
            align-items: flex-start;
            justify-content: center;
            height: auto;
            padding-top: 50px;
        }

        .container {
            display: flex;
            justify-content: space-between;
            background-color: #f7f9fc;
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            gap: 20px;
            width: 100%;
            max-width: 439px;
        }

        .form-container {
            flex: 1;
            background-color: #f5f5dc;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

            .form-container h2 {
                margin-bottom: 20px;
                font-size: 18px;
                color: #333;
            }

        .form-group {
            margin-bottom: 15px;
        }

        label {
            font-size: 14px;
            color: #555;
            display: block;
            margin-bottom: 8px;
        }

        input[type="date"] {
            width: 100%;
            padding: 12px;
            font-size: 14px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
            background-color: #f4f7f9;
        }

            input[type="date"]:focus {
                outline: none;
                border-color: #3498db;
                background-color: #ffffff;
            }

        button {
            width: 100%;
            padding: 12px;
            background-color: #3498db;
            color: #ffffff;
            font-size: 16px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

            button:hover {
                background-color: #2980b9;
            }

        .alert {
            color: #e74c3c;
            text-align: center;
            margin-top: 20px;
        }

        .alert-success {
            color: #2ecc71;
        }

        #priceDisplay {
            margin-top: 20px;
            font-size: 18px;
            text-align: center;
            color: #2c3e50;
            font-weight: bold;
        }

        .ticker-display-container {
            margin: 40px auto;
            padding: 20px;
            background-color: #f5f5dc;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            display: none;
            flex-direction: column;
            gap: 10px;
            max-width: 1200px;
            width: 100%;
        }


        .ticker-row {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            justify-content: center;
        }

        .ticker-box {
            background-color: #2980b9;
            color: #fff;
            padding: 10px;
            font-size: 16px;
            font-weight: bold;
            border-radius: 8px;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
            cursor: pointer;
        }

            .ticker-box:hover {
                transform: scale(1.1);
                background-color: #3498db;
            }

            .ticker-box:active {
                background-color: #2980b9;
            }

        .ticker-count-box {
            position: fixed;
            top: 20px;
            right: 20px;
            background-color: #2980b9;
            color: white;
            padding: 20px;
            border-radius: 10px;
            font-size: 18px;
            font-weight: bold;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            display: none;
            justify-content: center;
            align-items: center;
            min-width: 120px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Date Picker Form -->
        <div class="form-container">
            <h2>Select Date Range</h2>
            <form id="dateForm">
                <div class="form-group">
                    <label for="fromDate">From Date:</label>
                    <input type="date" id="fromDate" name="fromDate" required>
                </div>
                <div class="form-group">
                    <label for="toDate">To Date:</label>
                    <input type="date" id="toDate" name="toDate" required>
                </div>

                <!-- Sliding Buttons -->
                <div class="btn-container">
                    <button type="button" id="oneYearBtn">1 Year</button>
                    <button type="button" id="threeMonthsBtn">3 Months</button>
                </div>

                <button type="button" id="testButton">TEST</button>
            </form>
        </div>
    </div>

    <script>
        // Restrict the date pickers to prevent selecting tomorrow or later.
        document.addEventListener('DOMContentLoaded', function () {
            const today = new Date().toISOString().split('T')[0];
            document.getElementById('fromDate').max = today;
            document.getElementById('toDate').max = today;
        });

        document.getElementById('oneYearBtn').addEventListener('click', function() {
            var today = new Date();
            var oneYearAgo = new Date(today.setFullYear(today.getFullYear() - 1)); // 1 year ago
            var startDate = new Date(oneYearAgo.getFullYear(), 0, 1); // Start at January 1st of last year
            var endDate = new Date(); // Today's date

            // Format dates to YYYY-MM-DD
            var formattedStartDate = startDate.toISOString().split('T')[0];
            var formattedEndDate = endDate.toISOString().split('T')[0];

            document.getElementById('fromDate').value = formattedStartDate;
            document.getElementById('toDate').value = formattedEndDate;
        });

        document.getElementById('threeMonthsBtn').addEventListener('click', function() {
            var today = new Date();
            var threeMonthsAgo = new Date(today.setMonth(today.getMonth() - 3)); // 3 months ago
            var startDate = threeMonthsAgo; // 3 months ago date
            var endDate = new Date(); // Today's date

            // Format dates to YYYY-MM-DD
            var formattedStartDate = startDate.toISOString().split('T')[0];
            var formattedEndDate = endDate.toISOString().split('T')[0];

            document.getElementById('fromDate').value = formattedStartDate;
            document.getElementById('toDate').value = formattedEndDate;
        });

        // Handle the TEST button click
        document.getElementById('testButton').addEventListener('click', async function (event) {
            event.preventDefault(); // Prevent form submission

            const fromDate = document.getElementById('fromDate').value;
            const toDate = document.getElementById('toDate').value;

            var currentDate = new Date(fromDate);

            while (fromDate < toDate) {
                // Ensure the dates are not empty
                if (!fromDate || !toDate) {
                    alert('Please select both From Date and To Date.');
                    return;
                }

                const requestBody = {
                    startDate: currentDate, // Adjusted to match API model
                    endDate: currentDate
                };

                try {
                    // Make an API call
                    const response = await fetch('https://localhost:44364/api/Test/BulkTestMasterAPI', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify(requestBody)
                    });

                    currentDate.setDate(currentDate.getDate() + 1);

                    if (response.ok) {
                        const data = await response.json();
                        console.log('API Response:', data);
                    } else {
                        console.error('API Call Failed:', response.statusText);
                    }
                } catch (error) {
                    console.error('Error during API call:', error);
                }
            }
        });
    </script>

</body>
</html>
 *@



<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Date Picker Form</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f9fc;
            color: #333;
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        .container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            background-color: #f7f9fc;
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            gap: 20px;
            max-width: 368px;
            width: 100%;
        }

        .form-container {
            display: flex;
            align-items: center;
            justify-content: space-between;
            background-color: #f5f5dc;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .form-section {
            flex: 1;
        }

            .form-section h2 {
                margin-bottom: 20px;
                font-size: 18px;
                color: #333;
            }

        .form-group {
            margin-bottom: 15px;
        }

        label {
            font-size: 14px;
            color: #555;
            display: block;
            margin-bottom: 8px;
        }

        input[type="date"] {
            width: 100%;
            padding: 12px;
            font-size: 14px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
            background-color: #f4f7f9;
        }

            input[type="date"]:focus {
                outline: none;
                border-color: #3498db;
                background-color: #ffffff;
            }

        .btn-container {
            display: flex;
            flex-direction: column;
            gap: 10px;
            padding-left: 19px;
            margin-top: 62px;
        }

        button {
            padding: 12px;
            background-color: #3498db;
            color: #ffffff;
            font-size: 16px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

            button:hover {
                background-color: #2980b9;
            }

        .alert {
            color: #e74c3c;
            text-align: center;
            margin-top: 20px;
        }

        .alert-success {
            color: #2ecc71;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Date Picker Form -->
        <div class="form-container">
            <!-- Date Pickers Section -->
            <div class="form-section">
                <h2>Select Date Range</h2>
                <form id="dateForm">
                    <div class="form-group">
                        <label for="fromDate">From Date:</label>
                        <input type="date" id="fromDate" name="fromDate" required>
                    </div>
                    <div class="form-group">
                        <label for="toDate">To Date:</label>
                        <input type="date" id="toDate" name="toDate" required>
                    </div>
                </form>
            </div>

            <!-- Buttons Section -->
            <div class="btn-container">
                <button type="button" id="oneYearBtn">1 Year</button>
                <button type="button" id="threeMonthsBtn">3 Months</button>
                <button type="button" id="testButton">TEST</button>
            </div>
        </div>
    </div>

    <script>
        // Restrict the date pickers to prevent selecting tomorrow or later.
        document.addEventListener('DOMContentLoaded', function () {
            const today = new Date().toISOString().split('T')[0];
            document.getElementById('fromDate').max = today;
            document.getElementById('toDate').max = today;
        });

        document.getElementById('oneYearBtn').addEventListener('click', function() {
            var today = new Date();
            var oneYearAgo = new Date(today.setFullYear(today.getFullYear() - 1));
            var startDate = new Date(oneYearAgo.getFullYear(), 0, 1);
            var endDate = new Date();

            var formattedStartDate = startDate.toISOString().split('T')[0];
            var formattedEndDate = endDate.toISOString().split('T')[0];

            document.getElementById('fromDate').value = formattedStartDate;
            document.getElementById('toDate').value = formattedEndDate;
        });

        document.getElementById('threeMonthsBtn').addEventListener('click', function() {
            var today = new Date();
            var threeMonthsAgo = new Date(today.setMonth(today.getMonth() - 3));
            var formattedStartDate = threeMonthsAgo.toISOString().split('T')[0];
            var formattedEndDate = new Date().toISOString().split('T')[0];

            document.getElementById('fromDate').value = formattedStartDate;
            document.getElementById('toDate').value = formattedEndDate;
        });

        document.getElementById('testButton').addEventListener('click', async function (event) {
            event.preventDefault();

            const fromDate = document.getElementById('fromDate').value;
            const toDate = document.getElementById('toDate').value;

            if (!fromDate || !toDate) {
                alert('Please select both From Date and To Date.');
                return;
            }

            let currentDate = new Date(fromDate);
            const endDate = new Date(toDate);

            while (currentDate <= endDate) {
                const requestBody = {
                    startDate: currentDate.toISOString().split('T')[0],
                    endDate: currentDate.toISOString().split('T')[0]
                };

                try {
                    const response = await fetch('https://localhost:44364/api/Test/BulkTestMasterAPI', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify(requestBody)
                    });

                    currentDate.setDate(currentDate.getDate() + 1);

                    if (response.ok) {
                        const data = await response.json();
                        console.log('API Response:', data);
                    } else {
                        console.error('API Call Failed:', response.statusText);
                    }
                } catch (error) {
                    console.error('Error during API call:', error);
                }
            }

        });
    </script>
</body>
</html>
